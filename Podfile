# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'moviewall' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for moviewall
  pod 'SDWebImage', '~>3.8'
  pod 'Alamofire', '~> 4.0'

  target 'moviewallTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'moviewallUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
